import { createRequire } from "node:module";
import { spawn } from "node:child_process";
import { env } from "node:process";
import { K as runner, k as kill } from "./_shared.js";
import { L, M, N, O } from "./_shared.js";
const require2 = createRequire(import.meta.url), definePlugin = (plugin) => plugin, pokuBin = (() => {
  try {
    return require2.resolve("../bin/index.js");
  } catch {
    return "";
  }
})(), inspectPoku = (options) => {
  const { command, spawnOptions, bin = pokuBin } = options, ext = bin.endsWith(".ts") ? "ts" : "js", [runtime, ...runnerArgs] = runner(`_.${ext}`), basePath = typeof spawnOptions?.cwd == "string" ? spawnOptions.cwd.split(/\/|\\/).map(() => "../").join("") : "./", args = [...runnerArgs, `${basePath}${bin}`, ...command.split(" ")];
  return new Promise((resolve, reject) => {
    const childProcess = spawn(runtime, args, {
      ...spawnOptions,
      shell: !1,
      env: {
        ...spawnOptions?.env || env,
        POKU_RUNTIME: env.POKU_RUNTIME
      }
    });
    childProcess.stdout.setEncoding("utf8"), childProcess.stderr.setEncoding("utf8");
    const PID = childProcess.pid, kill$1 = async () => {
      await kill.pid(PID);
    };
    let stdout = "", stderr = "";
    childProcess.stdout.on("data", (data) => {
      stdout += String(data);
    }), childProcess.stderr.on("data", (data) => {
      stderr += String(data);
    }), childProcess.on("error", (error) => {
      reject({ error: error.message, stdout, stderr, exitCode: 1 });
    }), childProcess.on("close", (code) => {
      resolve({
        stdout,
        stderr,
        exitCode: code,
        process: childProcess,
        PID,
        kill: kill$1
      });
    });
  });
};
export {
  L as createReporter,
  definePlugin,
  M as findFileFromStack,
  inspectPoku,
  N as onSigint,
  O as reporterRegistry
};
